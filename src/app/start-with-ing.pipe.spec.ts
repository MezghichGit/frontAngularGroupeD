import { StartWithIngPipe } from './start-with-ing.pipe';

describe('StartWithIngPipe', () => {
  it('create an instance', () => {
    const pipe = new StartWithIngPipe();
    expect(pipe).toBeTruthy();
  });
});
